User: Dear Agent. This is an interactive document. Protocol: Work on phases that are not done yet.
When done, Write a progress report and name all involved modules/classes and add a note to this document: "DONE BY AGENT". You may also add small remarks and infos, but do not pullute the document. 
Summarization Protocol: Keep the document as single source of truth and when summarizing tell the pruned agent also where the doc lives. I need the whole session to visit this doc regularly!
All provided code snippets are examples, not mandatory code pieces. They serve as hints and are only examplatory in nature
Remember to write progress reports (there is a tool, just dump what you have there) after a chunk of work is done.

 Immediate Tasks for the Agent

I'll give you three self-contained tasks that the agent can execute sequentially. Each is small, testable, and builds toward full integration.
Task 1: Fix Indexing Process Hanging (Prevent Ghost Daemons)

File: agent/knowledge/codebase_indexer.py

What to change:

    In the embed_chunks_batched() function (or wherever the ChromaDB client is created), add explicit cleanup registration.

    Mark any custom threads as daemons.

Precise instructions for agent:

    Add at the top of the file: import atexit, signal, sys

    Locate the ChromaDB client instantiation (likely chromadb.PersistentClient(...)). Assign it to a variable that persists, e.g., _chroma_client.

    Define a cleanup function:

python

def _cleanup():
    if '_chroma_client' in globals() and _chroma_client:
        _chroma_client._system.stop()
    sys.exit(0)

    Register it: atexit.register(_cleanup)

    Also set signal handlers for SIGINT and SIGTERM:

python

signal.signal(signal.SIGINT, lambda sig, frame: _cleanup())
signal.signal(signal.SIGTERM, lambda sig, frame: _cleanup())

    Ensure any threading.Thread calls have daemon=True.

Verification: Run indexing and press Ctrl+C midway; check with pgrep -f indexer that no process remains.

DONE BY AGENT: Added atexit, signal handlers, and global client cleanup to codebase_indexer.py. Cleanup function stops ChromaDB system on exit.
Task 2: Update SearchCodebaseTool to Use Configurable Lightweight Model and Add Intent Parameter

Files: tools/search_codebase.py and agent_config.json

What to change:

    Switch the default embedding model to "BAAI/bge-small-en-v1.5".

    Add optional parameters intent and restrict_to_path to the tool's argument schema.

    Pass these to the underlying CodebaseKB.search() method (which may need corresponding updates—delegate that to agent's judgment).

Precise instructions for agent:

    Config file (agent_config.json): Change "rag_embedding_model" value to "BAAI/bge-small-en-v1.5".

    Tool arguments schema (in tools/search_codebase.py): Update the Pydantic model (SearchCodebaseToolArgs) to include:
    python

    intent: Optional[Literal["exact", "broad", "file"]] = "broad"
    restrict_to_path: Optional[str] = None

    Tool run method: In SearchCodebaseTool.run(), map intent to appropriate limit and min_score:

        "exact": limit=3, min_score=0.5

        "broad": limit=10, min_score=0.0

        "file": Use existing logic to aggregate chunks from a file (or simply return file paths).

    Filtering by restrict_to_path: If provided, add a metadata filter to the ChromaDB query. The exact syntax depends on CodebaseKB.search() implementation; if it doesn't support filters yet, add a where parameter to that method.

Verification: In the REPL, call search_codebase with intent="exact" and confirm it returns fewer, higher-confidence results.
Task 3: Make Chunk Size and Model Choice Configurable via agent_config.json

File: agent/config/models.py and agent_config.json

What to change:

    Add two new fields to AgentConfig:
    python

    rag_batch_size: int = Field(default=16, description="Batch size for embedding generation")
    rag_truncate_dim: Optional[int] = Field(default=None, description="Truncate embeddings to this dimension")

    Update agent_config.json with default values.

    Modify codebase_indexer.py to read these values from the config and pass them to embed_chunks_batched().

Verification: After changes, indexing should respect the new batch size and truncation dimension from the config file.


The next step is to avoid re-indexing the entire codebase every time. This will make the system feel snappy and usable during development.

Task 4: Create an Incremental Re-indexing Tool

Goal: Add a new CLI command (or tool) that updates the vector store only for files that have changed since the last index.

Precise instructions for the agent:

    Add a new CLI command in agent/cli/rag_commands.py:

        Command name: update-index

        Should call a new function in codebase_indexer.py, e.g., incremental_index(workspace_path, config)

    In codebase_indexer.py, implement incremental_index():

        Maintain a metadata file (e.g., chroma_data/.index_state.json) that stores for each file: file_path, last_modified timestamp, and a file_hash (optional).

        During incremental indexing:

            Scan workspace for all code files (using same extensions filter).

            Compare current last_modified with stored value.

            For new or modified files: chunk, embed, and upsert into ChromaDB (overwriting any existing chunks for that file).

            For deleted files: remove their chunks from ChromaDB using a metadata filter on file_path.

        Update the state file after completion.

    Modify full index command to also write the initial state file.

    Expose as an optional agent tool? Later, but for now just the CLI is fine. The agent can call the CLI via subprocess when needed.

Why this matters: It turns a 2-minute full index into a 5-second update after editing a few files.
📋 Task 5 (Optional but Recommended): Staleness Detection in Agent Startup

Once incremental indexing exists, the agent can be made aware of codebase changes.

Instructions for agent:

    In agent/core/agent.py (or wherever initialization happens), after loading the CodebaseKB, compare the stored index hash with the current workspace hash.

    If different, add a system message to the agent's initial prompt: "The codebase has changed since the last index. You may want to suggest running update-index for accurate search results."

    The agent can then, when appropriate, inform the user or call the update tool itself.

✅ Summary for the Agent's Next Sprint

    Task 4: Incremental re-indexing – Implement update-index CLI command and incremental_index() function.

    Task 5: Staleness awareness – Add hash check at startup with a system message.

    Task 6 (later): GUI integration – Add a button and notification area.

Each task is self-contained and builds on the previous work.