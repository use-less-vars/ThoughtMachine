# ⚠️ OBSOLETE — This document describes a pre-migration Qt architecture

This file is a stale artifact from the Qt-based design phase. It refers to `CapabilityRegistry`, `requires_capabilities`, "no GUI subscription," and Qt GUI plans — none of which exist in the current codebase.

**The canonical security architecture document is at:**

```
.thoughtmachine/knowledge/project/security_architecture.md
```

**Also available in the global knowledge base after bootstrap:**

```
~/.thoughtmachine/knowledge/system/security_architecture.md
```

(If the global KB path doesn't exist yet, run `bootstrap.py` or wait for the next version sync — the file is now shipped as a factory default.)

(If you're reading this from a fresh clone, run the agent and use `KnowledgeBaseTool mode=read domain=security_architecture scope=workspace` to view it.)

---

*This file was kept as a redirect to prevent confusion. The original contents described an abandoned design that does not match the live system.*
